import React from 'react';

const RecordatorioEvento = ({ address }) => {
  return (
    <div>
      <h1>Recordatorio</h1>
      <p>Este evento será en: {address}</p>
    </div>
  );
};

export default RecordatorioEvento;
